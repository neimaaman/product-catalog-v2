// in js get the HTML element that we want to watch
// in js add some EventListners that watches 
// in js when a form submit happens, get all the va value

const productForm = document.querySelector;//document object refers to the whole page of the website we r working on 